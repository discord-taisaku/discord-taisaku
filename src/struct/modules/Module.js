module.exports = class TaisakuModule {
    constructor({ category = 'default' } = {}) {
        
    };

    /**
     * Returns the _ of the module.
     * @returns {string}
     */
    toString() {
        return ''
    };
};